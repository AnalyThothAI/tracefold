# Every mutant that survives the batch in `mutation.toml`, and why it is allowed to.
#
# `scripts/mutation_survivors.py` fails the lane on a survivor that is not covered here, on an entry
# that no longer matches a survivor, and on a rule that matches none. An exemption that outlives its
# reason is how a gate stops gating while still looking like one.

# ---------------------------------------------------------------------------------------------
# Rules: mechanical categories, accepted only where the premise is checked against the source.
# ---------------------------------------------------------------------------------------------

[[rule]]
kind = "annotation-union"
operator = "core/ReplaceBinaryOperator_BitOr_.*"
reason = """
A `|` inside a type annotation, in a module with `from __future__ import annotations`. The
annotation is stored as a string and never evaluated, so replacing the union operator cannot
change observable behaviour. The classifier parses the module and accepts a mutation only on a
line where no `|` is evaluated.
"""

# ---------------------------------------------------------------------------------------------
# Sites: one entry per mutation site, written by hand.
# ---------------------------------------------------------------------------------------------

# @dataclass(frozen=True, slots=False)
[[accepted]]
module = "tracefold/trading/market_context.py"
function = "<module>"
line = 38
operator = "core/ReplaceTrueWithFalse"
occurrence = 1
reason = """
`slots=True` becomes `slots=False`. That changes how instances are laid out in memory and nothing
else: the class is still `frozen=True`, so attribute assignment still raises, and no test can
observe the difference. The immutability half of the same decorator is covered directly.
"""

# def select_bar(bars: Sequence[Bar], /, target_ms: int, gap_tolerance_ms: int) -> Bar | None:
[[accepted]]
module = "tracefold/trading/market_context.py"
function = "select_bar"
line = 55
operator = "core/ReplaceBinaryOperator_Mul_Div"
occurrence = 0
reason = """
Cosmic Ray reads the bare `*` that opens a keyword-only section as a multiplication operator and
rewrites it to `/`, which is the positional-only marker. Existing calls pass the parameters by
keyword, so the mutation cannot change their binding.
"""

# if best is None or int(target_ms) % best.close_at_ms > int(gap_tolerance_ms):
[[accepted]]
module = "tracefold/trading/market_context.py"
function = "select_bar"
line = 67
operator = "core/ReplaceBinaryOperator_Sub_Mod"
occurrence = 0
reason = """
`a - b` becomes `a % b`, on epoch timestamps where `b` is within a small window of `a`. For
non-negative integers with `b > a / 2`, the expressions are equal. Inputs old enough to separate
them are rejected by the staleness and gap-tolerance bounds before this result is used.
"""

# /,
[[accepted]]
module = "tracefold/trading/market_context.py"
function = "pre_move_bps"
line = 91
operator = "core/ReplaceBinaryOperator_Mul_Div"
occurrence = 2
reason = """
Cosmic Ray reads the bare `*` that opens a keyword-only section as a multiplication operator and
rewrites it to `/`, which is the positional-only marker. Existing calls pass the parameters by
keyword, so the mutation cannot change their binding.
"""
