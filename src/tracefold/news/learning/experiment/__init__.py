"""The operator's fast research loop: snapshot, compare, optimize.

Read-only against the database, file-backed, and structurally unable to promote anything. It shares the
metric, the episode projection and the GEPA core with the release plane precisely so that what it
measures predicts what a trusted compile will do — and shares nothing else, so a research run can never
become release evidence.
"""

from __future__ import annotations

__all__: list[str] = []
